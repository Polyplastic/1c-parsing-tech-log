
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	Если ОбновлениеДанных.ИнформационнаяБазаФайловая() Тогда
		ОбновлениеДанных.ПрочитатьЖурнал();
	Иначе
		ОбновлениеДанных.ПрочитатьЖурналВФоне();
	КонецЕсли;
КонецПроцедуры

