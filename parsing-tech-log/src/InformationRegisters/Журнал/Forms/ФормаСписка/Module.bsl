
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	ОткрытьФорму("Обработка.ПросмотрТехнологическогоЖурнала.Форма.Форма", ПараметрыФормы);
	Отказ = Истина;	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	ПараметрыФормы = Новый Структура("Отбор", Новый Структура);
	Для Каждого пар из ЭтаФорма.Параметры.Отбор Цикл
		ПараметрыФормы.Отбор.Вставить(пар.Ключ, пар.Значение);
	КонецЦикла;
КонецПроцедуры
